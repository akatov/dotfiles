export AWS_ACCESS_KEY='{{- (onepassword "AWS Access Key").details.password -}}'
export AWS_SECRET_KEY='{{- (onepassword "AWS Secret Key").details.password -}}'
export AWS_SSH_KEY_FILE='{{- (onepassword "AWS SSH Key File").details.password -}}'
export AWS_KEYPAIR_NAME='{{- (onepassword "AWS Keypair Name").details.password -}}'
