export AWS_ACCESS_KEY="{{ template "chezmoi/secret" "aws access key" }}"
export AWS_SECRET_KEY="{{ template "chezmoi/secret" "aws secret key" }}"
export AWS_SSH_KEY_FILE="{{ template "chezmoi/secret" "aws ssh key file" }}"
export AWS_KEYPAIR_NAME="{{ template "chezmoi/secret" "aws keypair name" }}"
