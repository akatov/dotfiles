{{- if .personal -}}
{
  "auth": {
    "floobits.com": {
      "username": "akatov",
      "api_key": "{{ template "_secret" (set . "secret" "floobits key") }}",
      "secret": "{{ template "_secret" (set . "secret" "floobits secret") }}"
    }
  }
}
{{ end }}